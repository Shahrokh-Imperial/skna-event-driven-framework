# Synthetic example

The files in this directory are generated, non-experimental data supplied only to demonstrate the command-line workflow. SKNA columns are stored in mV, matching the processed cohort convention, and are converted internally to µV.
