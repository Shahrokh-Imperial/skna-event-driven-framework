# GitHub and Zenodo release

Create a GitHub release tagged `v1.0.0`, enable the repository in the Zenodo GitHub integration, and archive the tagged release. Add the resulting DOI to `CITATION.cff` and the README in a metadata-only follow-up release if required.
