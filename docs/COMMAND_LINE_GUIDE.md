# Command-line guide

Run each script with `--help` for all options. The typical sequence is preprocessing, event definition, threshold estimation, sequential replay, and event summary. The end-to-end script accepts a processed CSV and event CSV.
